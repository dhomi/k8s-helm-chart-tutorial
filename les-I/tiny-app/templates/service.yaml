apiVersion: v1
kind: Service
metadata:
  labels:
    app: tiny-app
  name: tiny-app
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: tiny-app
  sessionAffinity: None
  type: LoadBalancer

