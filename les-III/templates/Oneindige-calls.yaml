apiVersion: apps/v1
kind: Deployment
metadata:
  name: oneindige-calls
  labels:
    app: oneindige-calls
spec:
  replicas: 5
  selector:
    matchLabels:
      app: oneindige-calls
  template:
    metadata:
      name: oneindige-calls
      labels:
        app: oneindige-calls
    spec:
      terminationGracePeriodSeconds: 3
      containers:
      - name: oneindige-calls
        image: busybox
        command:
        - /bin/sh
        - -c
        - "while true; do wget -q -O- http://php-apache; done"